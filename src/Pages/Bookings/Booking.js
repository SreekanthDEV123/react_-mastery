import React from "react";

const Booking = () => {
  const randomNumber = Math.round(10 * Math.random());
  console.log("ra", randomNumber);
  return (
    <div>
      <h1>Hai its coming</h1>
    </div>
  );
};

export default Booking;
